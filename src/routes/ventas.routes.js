import { Router } from "express";
const routerVentas = Router();
import controller from "../controllers/ventas.controller";

routerUsuarios.get("api/ventas", (req, res) => res.send("loros"));

export default routerVentas;
