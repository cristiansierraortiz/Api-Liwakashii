import { Router } from "express";
const routerTipoProducto = Router();
import controller from "../controllers/tipo-producto.controller";

routerUsuarios.get("api/tipo-producto", (req, res) => res.send("loros"));

export default routerTipoProducto;
